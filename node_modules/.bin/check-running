#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../check-running/bin/check-running" "$@"
  ret=$?
else 
  node  "$basedir/../check-running/bin/check-running" "$@"
  ret=$?
fi
exit $ret
